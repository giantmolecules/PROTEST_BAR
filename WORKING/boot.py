import network
import os

#ap1 = network.WLAN(network.AP_IF)
#ap2 = network.WLAN(network.AP_IF)

#ap1.active(True)
#ap1.config(essid="ap1", authmode=0)
#ap2.active(True)
#ap2.config(essid="ap2", authmode=0)
